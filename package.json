{
  "name": "root",
  "private": true,
  "type": "commonjs",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "lerna run build",
    "depcheck": "lerna run depcheck",
    "deploy": "lerna run deploy",
    "format": "lerna run format",
    "test": "lerna run test",
    "test:engines": "lerna run test:engines",
    "version": ""
  },
  "devDependencies": {
    "lerna": "^8.1.9"
  }
}
