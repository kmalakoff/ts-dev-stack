{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "ep -e -d1 -c100 npm run build",
    "format": "ep -d1 -c100 npm run format",
    "publish": "ep -d1 -c1 tsds publish",
    "test": "ep -d1 -c100 npm run test",
    "test:engines": "ep -d1 -c100 npm run test:engines",
    "version": "ep -d1 -c100 npm run version"
  },
  "dependencies": {
    "cross-spawn-cb": "^2.4.1",
    "each-package": "^1.2.2",
    "playwright": "^1.53.1"
  }
}
